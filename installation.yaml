# For documentation and available settings, see
# https://github.com/srid/emanote/blob/master/default/index.yaml

page:
  siteTitle: Haskell HOWTOs

template:
  theme: green
  sidebar:
    collapsed: false
  urlStrategy: pretty

pandoc:
  rewriteClass:
    header: font-bold
    normal: font-normal
    red: text-red-700
